generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model User {
  id String @id @default(cuid()) @map("_id")
  special_id String @unique
  first_name String 
  last_name String
  email String @unique
  avatar String?
}